# T1 回归网、依赖分层与 CI 基线报告

日期：2026-08-02

## 本次落地

新增了核心/开发/可选依赖清单、pytest 配置、随机端口测试启动器、HTTP 辅助函数、数据库状态 fixture、六条路由 smoke test、六份 HTML 快照和 GitHub Actions CI 配置。

本次没有修改 `app.py`，没有删除或覆盖现有研究数据、数据库、脚本和日志；当前工作树中原有的未提交改动保持不变。

## 依赖口径

依赖清单按仓库 Python 文件的真实 import 扫描整理：

- 核心运行：`zhconv==1.4.3`。
- 开发测试：`pytest==8.4.2`，测试 HTTP 请求使用 Python 标准库，不额外引入 requests。
- 可选处理：BeautifulSoup、Pillow、PyMuPDF、pypdf、pypdfium2、PaddleOCR/PaddlePaddle、python-docx、openpyxl、requests 等，统一放在 `requirements-optional.txt`。
- `argostranslate` 和 `weasyprint` 当前环境未安装，因此只记录为待单独验证项，没有凭空指定版本。

## 本地真实数据验收

当前本地数据库状态：

| 项目 | 数量 |
|---|---:|
| documents | 1,114 |
| pages | 5,475 |
| translations | 1,075 |
| research_events | 1,914 |
| domestic_candidates | 689 |
| domestic_sources | 89 |

文档详情快照使用 `ORDER BY doc_key LIMIT 1` 取得真实文档键：
`cia-meng:CIA-RDP82-00457R002800130003-8`。

六条路由均取得真实 200 响应并建立快照：

`/`、`/dashboard`、`/sourcebooks`、`/doc/<doc_key>`、`/timeline`、`/domestic`。

快照只规范化静态资源 mtime 查询参数；文档页另将当天引用日期规范化为 `TODAY`。没有替换历史日期、研究正文或数据库内容。

## 测试结果

首次运行生成六份快照基线；第二次独立比较结果：

```text
12 passed in 0.50s
```

另外完成了五个测试模块的无写入编译检查和 `git diff --check`，均通过。

第一次在受限沙箱中运行时，系统禁止子进程监听随机 localhost 端口，出现 `PermissionError: [Errno 1] Operation not permitted`；随后在允许临时 localhost 监听的执行环境中重新运行，测试通过。测试服务只使用随机临时端口，结束后自动关闭。

## CI 边界与未完成项

- CI 只安装核心开发依赖，不安装 OCR/PDF/翻译等重型可选依赖。
- `data/research_index.sqlite` 与国内 staging SQLite 均属于本地研究状态，不进 git；全新 clone 的 CI 中，5 个真实数据库页面路由会按 fixture 明确 skip，而不会伪装成通过。`/sourcebooks` 不依赖数据库，仍可独立验证。
- 本次未 push，也未在 GitHub Actions 上触发真实运行；远程权限和 Actions 绿勾仍待后续明确授权后验证。
